# Import     
import mysql.connector 

def sAg_rounder(leagueapi_id, null_status, space):
    # ----------------------------------------------------------   
    # 1a. Match Update
    # ----------------------------------------------------------   
    space += "__"
    # ---------------------------------------------------------- 
    print(space + "sAg_rounder()", flush=True)
    # ----------------------------------------------------------  
    host="localhost"
    user="root" 
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ---------------------------------------------------------- 
    query = " Select "
    query += " detail_stats "  
    query += " from football_leagues " 
    query += " where leagueapi_id = '"+str(leagueapi_id)+"' " 
    # ----------------------------------------------------------   
    # print(space + query)
    # ----------------------------------------------------------   
    mycursor = mydb.cursor()
    mycursor.execute(query)
    result =  mycursor.fetchall()
    # ---------------------------------------------------------- 
    total_rows = len(result)
    # print(space + "Total Row(s) : " + str(total_rows), flush=True) 
    # close the cursor and database connection
    mycursor.close()
    mydb.close()
    # ---------------------------------------------------------- 
    counter = 0
    # ----------------------------------------------------------   
    for x in result:    
        # ------------------------------------------------------
        counter        += 1
        detail_stats   = str(x[0])  
    # ---------------------------------------------------------- 
    # ---------------------------------------------------------- sAg_score_secondtime 
    sAg_score_secondtime(leagueapi_id, null_status, space)
    print("", flush=True)

    # ---------------------------------------------------------- Match Winner
    sAg_match_winner_home(leagueapi_id, null_status, space)
    sAg_zero("match_winner_home", space)
    sAg_match_winner_draw(leagueapi_id, null_status, space)
    sAg_zero("match_winner_draw", space)
    sAg_match_winner_away(leagueapi_id, null_status, space)
    sAg_zero("match_winner_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Home/Away
    sAg_homeaway_home(leagueapi_id, null_status, space)
    sAg_zero("homeaway_home", space)
    sAg_homeaway_away(leagueapi_id, null_status, space)
    sAg_zero("homeaway_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Second Half Winner
    sAg_second_half_winner_home(leagueapi_id, null_status, space)
    sAg_zero("second_half_winner_home", space)
    sAg_second_half_winner_draw(leagueapi_id, null_status, space)
    sAg_zero("second_half_winner_draw", space)
    sAg_second_half_winner_away(leagueapi_id, null_status, space)
    sAg_zero("second_half_winner_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Asian Handicap
    sAg_asian_handicap_home_min_675(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_675", space)
    sAg_asian_handicap_away_min_675(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_675", space)
    sAg_asian_handicap_home_min_65(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_65", space)
    sAg_asian_handicap_away_min_65(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_65", space)
    sAg_asian_handicap_home_min_625(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_625", space)
    sAg_asian_handicap_away_min_625(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_625", space)
    sAg_asian_handicap_home_min_6(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_6", space)
    sAg_asian_handicap_away_min_6(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_6", space)
    sAg_asian_handicap_home_min_575(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_575", space)
    sAg_asian_handicap_away_min_575(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_575", space)
    sAg_asian_handicap_home_min_55(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_55", space)
    sAg_asian_handicap_away_min_55(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_55", space)
    sAg_asian_handicap_home_min_525(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_525", space)
    sAg_asian_handicap_away_min_525(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_525", space)
    sAg_asian_handicap_home_min_5(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_5", space)
    sAg_asian_handicap_away_min_5(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_5", space)
    sAg_asian_handicap_home_min_475(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_475", space)
    sAg_asian_handicap_away_min_475(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_475", space)
    sAg_asian_handicap_home_min_45(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_45", space)
    sAg_asian_handicap_away_min_45(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_45", space)
    sAg_asian_handicap_home_min_425(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_425", space)
    sAg_asian_handicap_away_min_425(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_425", space)
    sAg_asian_handicap_home_min_4(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_4", space)
    sAg_asian_handicap_away_min_4(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_4", space)
    sAg_asian_handicap_home_min_375(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_375", space)
    sAg_asian_handicap_away_min_375(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_375", space)
    sAg_asian_handicap_home_min_35(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_35", space)
    sAg_asian_handicap_away_min_35(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_35", space)
    sAg_asian_handicap_home_min_325(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_325", space)
    sAg_asian_handicap_away_min_325(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_325", space)
    sAg_asian_handicap_home_min_3(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_3", space)
    sAg_asian_handicap_away_min_3(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_3", space)
    sAg_asian_handicap_home_min_275(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_275", space)
    sAg_asian_handicap_away_min_275(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_275", space)
    sAg_asian_handicap_home_min_25(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_25", space)
    sAg_asian_handicap_away_min_25(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_25", space)
    sAg_asian_handicap_home_min_225(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_225", space)
    sAg_asian_handicap_away_min_225(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_225", space)
    sAg_asian_handicap_home_min_2(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_2", space)
    sAg_asian_handicap_away_min_2(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_2", space)
    sAg_asian_handicap_home_min_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_175", space)
    sAg_asian_handicap_away_min_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_175", space)
    sAg_asian_handicap_home_min_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_15", space)
    sAg_asian_handicap_away_min_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_15", space)
    sAg_asian_handicap_home_min_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_125", space)
    sAg_asian_handicap_away_min_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_125", space)
    sAg_asian_handicap_home_min_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_1", space)
    sAg_asian_handicap_away_min_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_1", space)
    sAg_asian_handicap_home_min_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_075", space)
    sAg_asian_handicap_away_min_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_075", space)
    sAg_asian_handicap_home_min_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_05", space)
    sAg_asian_handicap_away_min_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_05", space)
    sAg_asian_handicap_home_min_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_min_025", space)
    sAg_asian_handicap_away_min_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_min_025", space)
    sAg_asian_handicap_home_plus_0(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_0", space)
    sAg_asian_handicap_away_plus_0(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_0", space)
    sAg_asian_handicap_home_plus_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_025", space)
    sAg_asian_handicap_away_plus_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_025", space)

    sAg_asian_handicap_home_plus_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_05", space)
    sAg_asian_handicap_away_plus_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_05", space)
    sAg_asian_handicap_home_plus_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_075", space)
    sAg_asian_handicap_away_plus_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_075", space)
    sAg_asian_handicap_home_plus_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_1", space)
    sAg_asian_handicap_away_plus_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_1", space)
    sAg_asian_handicap_home_plus_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_125", space)
    sAg_asian_handicap_away_plus_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_125", space)
    sAg_asian_handicap_home_plus_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_15", space)
    sAg_asian_handicap_away_plus_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_15", space)
    sAg_asian_handicap_home_plus_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_175", space)
    sAg_asian_handicap_away_plus_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_175", space)
    sAg_asian_handicap_home_plus_2(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_2", space)
    sAg_asian_handicap_away_plus_2(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_2", space)
    sAg_asian_handicap_home_plus_225(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_225", space)
    sAg_asian_handicap_away_plus_225(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_225", space)
    sAg_asian_handicap_home_plus_25(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_25", space)
    sAg_asian_handicap_away_plus_25(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_25", space)
    sAg_asian_handicap_home_plus_275(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_275", space)
    sAg_asian_handicap_away_plus_275(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_275", space)
    sAg_asian_handicap_home_plus_3(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_3", space)
    sAg_asian_handicap_away_plus_3(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_3", space)
    sAg_asian_handicap_home_plus_325(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_325", space)
    sAg_asian_handicap_away_plus_325(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_325", space)
    sAg_asian_handicap_home_plus_35(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_35", space)
    sAg_asian_handicap_away_plus_35(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_35", space)
    sAg_asian_handicap_home_plus_375(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_375", space)
    sAg_asian_handicap_away_plus_375(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_375", space)
    sAg_asian_handicap_home_plus_4(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_4", space)
    sAg_asian_handicap_away_plus_4(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_4", space)
    sAg_asian_handicap_home_plus_425(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_425", space)
    sAg_asian_handicap_away_plus_425(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_425", space)
    sAg_asian_handicap_home_plus_45(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_45", space)
    sAg_asian_handicap_away_plus_45(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_45", space)
    sAg_asian_handicap_home_plus_475(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_475", space)
    sAg_asian_handicap_away_plus_475(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_475", space)
    sAg_asian_handicap_home_plus_5(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_5", space)
    sAg_asian_handicap_away_plus_5(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_5", space)
    sAg_asian_handicap_home_plus_525(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_525", space)
    sAg_asian_handicap_away_plus_525(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_525", space)
    sAg_asian_handicap_home_plus_55(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_55", space)
    sAg_asian_handicap_away_plus_55(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_55", space)
    sAg_asian_handicap_home_plus_575(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_575", space)
    sAg_asian_handicap_away_plus_575(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_575", space)
    sAg_asian_handicap_home_plus_6(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_6", space)
    sAg_asian_handicap_away_plus_6(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_6", space)
    sAg_asian_handicap_home_plus_625(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_625", space)
    sAg_asian_handicap_away_plus_625(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_625", space)
    sAg_asian_handicap_home_plus_65(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_65", space)
    sAg_asian_handicap_away_plus_65(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_65", space)
    sAg_asian_handicap_home_plus_675(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_home_plus_675", space)
    sAg_asian_handicap_away_plus_675(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_away_plus_675", space)
    print("", flush=True)

    # ---------------------------------------------------------- Goals Over/Under
    sAg_goals_overunder_over_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_05", space)
    sAg_goals_overunder_under_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_05", space)
    sAg_goals_overunder_over_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_075", space)
    sAg_goals_overunder_under_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_075", space)
    sAg_goals_overunder_over_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_10", space)
    sAg_goals_overunder_under_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_10", space)
    sAg_goals_overunder_over_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_125", space)
    sAg_goals_overunder_under_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_125", space)
    sAg_goals_overunder_over_15(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_15", space)
    sAg_goals_overunder_under_15(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_15", space)
    sAg_goals_overunder_over_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_175", space)
    sAg_goals_overunder_under_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_175", space)
    sAg_goals_overunder_over_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_20", space)
    sAg_goals_overunder_under_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_20", space)
    sAg_goals_overunder_over_225(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_225", space)
    sAg_goals_overunder_under_225(leagueapi_id, null_status, space)

    sAg_zero("goals_overunder_under_225", space)
    sAg_goals_overunder_over_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_25", space)
    sAg_goals_overunder_under_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_25", space)
    sAg_goals_overunder_over_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_275", space)
    sAg_goals_overunder_under_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_275", space)
    sAg_goals_overunder_over_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_30", space)
    sAg_goals_overunder_under_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_30", space)
    sAg_goals_overunder_over_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_325", space)
    sAg_goals_overunder_under_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_325", space)
    sAg_goals_overunder_over_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_35", space)
    sAg_goals_overunder_under_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_35", space)
    sAg_goals_overunder_over_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_375", space)
    sAg_goals_overunder_under_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_375", space)
    sAg_goals_overunder_over_40(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_40", space)
    sAg_goals_overunder_under_40(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_40", space)
    sAg_goals_overunder_over_425(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_425", space)
    sAg_goals_overunder_under_425(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_425", space)
    sAg_goals_overunder_over_45(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_45", space)
    sAg_goals_overunder_under_45(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_45", space)
    sAg_goals_overunder_over_475(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_475", space)
    sAg_goals_overunder_under_475(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_475", space)
    sAg_goals_overunder_over_50(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_50", space)
    sAg_goals_overunder_under_50(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_50", space)
    sAg_goals_overunder_over_525(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_525", space)
    sAg_goals_overunder_under_525(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_525", space)
    sAg_goals_overunder_over_55(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_55", space)
    sAg_goals_overunder_under_55(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_55", space)
    sAg_goals_overunder_over_575(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_575", space)
    sAg_goals_overunder_under_575(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_575", space)
    sAg_goals_overunder_over_60(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_60", space)
    sAg_goals_overunder_under_60(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_60", space)
    sAg_goals_overunder_over_625(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_625", space)
    sAg_goals_overunder_under_625(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_625", space)
    sAg_goals_overunder_over_65(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_65", space)
    sAg_goals_overunder_under_65(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_65", space)
    sAg_goals_overunder_over_675(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_675", space)
    sAg_goals_overunder_under_675(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_675", space)
    sAg_goals_overunder_over_70(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_70", space)
    sAg_goals_overunder_under_70(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_70", space)
    sAg_goals_overunder_over_75(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_75", space)
    sAg_goals_overunder_under_75(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_75", space)
    sAg_goals_overunder_over_85(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_85", space)
    sAg_goals_overunder_under_85(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_85", space)
    sAg_goals_overunder_over_95(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_over_95", space)
    sAg_goals_overunder_under_95(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_under_95", space)
    print("", flush=True)

    # ---------------------------------------------------------- Goals Over/Under First Half
    sAg_goals_overunder_first_half_over_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_05", space)
    sAg_goals_overunder_first_half_under_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_05", space)
    sAg_goals_overunder_first_half_over_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_075", space)
    sAg_goals_overunder_first_half_under_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_075", space)
    sAg_goals_overunder_first_half_over_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_10", space)
    sAg_goals_overunder_first_half_under_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_10", space)
    sAg_goals_overunder_first_half_over_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_125", space)
    sAg_goals_overunder_first_half_under_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_125", space)
    sAg_goals_overunder_first_half_over_15(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_15", space)
    sAg_goals_overunder_first_half_under_15(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_15", space)
    sAg_goals_overunder_first_half_over_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_175", space)
    sAg_goals_overunder_first_half_under_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_175", space)
    sAg_goals_overunder_first_half_over_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_20", space)
    sAg_goals_overunder_first_half_under_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_20", space)
    sAg_goals_overunder_first_half_over_225(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_225", space)
    sAg_goals_overunder_first_half_under_225(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_225", space)
    sAg_goals_overunder_first_half_over_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_25", space)
    sAg_goals_overunder_first_half_under_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_25", space)
    sAg_goals_overunder_first_half_over_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_275", space)
    sAg_goals_overunder_first_half_under_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_275", space)
    sAg_goals_overunder_first_half_over_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_30", space)
    sAg_goals_overunder_first_half_under_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_30", space)

    sAg_goals_overunder_first_half_over_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_325", space)
    sAg_goals_overunder_first_half_under_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_325", space)
    sAg_goals_overunder_first_half_over_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_35", space)
    sAg_goals_overunder_first_half_under_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_35", space)
    sAg_goals_overunder_first_half_over_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_over_375", space)
    sAg_goals_overunder_first_half_under_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder_first_half_under_375", space)
    print("", flush=True)

    # ---------------------------------------------------------- HT/FT Double
    sAg_htft_double_home_home(leagueapi_id, null_status, space)
    sAg_zero("htft_double_home_home", space)
    sAg_htft_double_home_draw(leagueapi_id, null_status, space)
    sAg_zero("htft_double_home_draw", space)
    sAg_htft_double_home_away(leagueapi_id, null_status, space)
    sAg_zero("htft_double_home_away", space)
    sAg_htft_double_draw_home(leagueapi_id, null_status, space)
    sAg_zero("htft_double_draw_home", space)
    sAg_htft_double_draw_draw(leagueapi_id, null_status, space)
    sAg_zero("htft_double_draw_draw", space)
    sAg_htft_double_draw_away(leagueapi_id, null_status, space)
    sAg_zero("htft_double_draw_away", space)
    sAg_htft_double_away_home(leagueapi_id, null_status, space)
    sAg_zero("htft_double_away_home", space)
    sAg_htft_double_away_draw(leagueapi_id, null_status, space)
    sAg_zero("htft_double_away_draw", space)
    sAg_htft_double_away_away(leagueapi_id, null_status, space)
    sAg_zero("htft_double_away_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Both Teams Score
    sAg_both_teams_score_yes(leagueapi_id, null_status, space)
    sAg_zero("both_teams_score_yes", space)
    sAg_both_teams_score_no(leagueapi_id, null_status, space)
    sAg_zero("both_teams_score_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Highest Scoring Half
    sAg_highest_scoring_half_first(leagueapi_id, null_status, space)
    sAg_zero("highest_scoring_half_first", space)
    sAg_highest_scoring_half_draw(leagueapi_id, null_status, space)
    sAg_zero("highest_scoring_half_draw", space)
    sAg_highest_scoring_half_second(leagueapi_id, null_status, space)
    sAg_zero("highest_scoring_half_second", space)
    print("", flush=True)

    # ---------------------------------------------------------- Double Chance
    sAg_double_chance_home_draw(leagueapi_id, null_status, space)
    sAg_zero("double_chance_home_draw", space)
    sAg_double_chance_home_away(leagueapi_id, null_status, space)
    sAg_zero("double_chance_home_away", space)
    sAg_double_chance_draw_away(leagueapi_id, null_status, space)
    sAg_zero("double_chance_draw_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- First Half Winner
    sAg_first_half_winner_home(leagueapi_id, null_status, space)
    sAg_zero("first_half_winner_home", space)
    sAg_first_half_winner_draw(leagueapi_id, null_status, space)
    sAg_zero("first_half_winner_draw", space)
    sAg_first_half_winner_away(leagueapi_id, null_status, space)
    sAg_zero("first_half_winner_away", space)
    print("", flush=True)

    # # ---------------------------------------------------------- Team To Score First
    # sAg_team_to_score_first_home(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_first_home", space)
    # sAg_team_to_score_first_draw(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_first_draw", space)
    # sAg_team_to_score_first_away(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_first_away", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Team To Score Last
    # sAg_team_to_score_last_home(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_last_home", space)
    # sAg_team_to_score_last_draw(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_last_draw", space)
    # sAg_team_to_score_last_away(leagueapi_id, null_status, space)
    # sAg_zero("team_to_score_last_away", space)
    # print("", flush=True)

    # ---------------------------------------------------------- Total - Home
    sAg_total_home_over_15(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_15", space)
    sAg_total_home_under_15(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_15", space)
    sAg_total_home_over_25(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_25", space)
    sAg_total_home_under_25(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_25", space)
    sAg_total_home_over_35(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_35", space)
    sAg_total_home_under_35(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_35", space)
    sAg_total_home_over_45(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_45", space)
    sAg_total_home_under_45(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_45", space)
    sAg_total_home_over_55(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_55", space)
    sAg_total_home_under_55(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_55", space)
    sAg_total_home_over_65(leagueapi_id, null_status, space)
    sAg_zero("total_home_over_65", space)
    sAg_total_home_under_65(leagueapi_id, null_status, space)
    sAg_zero("total_home_under_65", space)
    print("", flush=True)

    # ---------------------------------------------------------- Total - Away
    sAg_total_away_over_15(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_15", space)
    sAg_total_away_under_15(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_15", space)
    sAg_total_away_over_25(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_25", space)
    sAg_total_away_under_25(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_25", space)
    sAg_total_away_over_35(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_35", space)
    sAg_total_away_under_35(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_35", space)
    sAg_total_away_over_45(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_45", space)
    sAg_total_away_under_45(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_45", space)
    sAg_total_away_over_55(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_55", space)
    sAg_total_away_under_55(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_55", space)
    sAg_total_away_over_65(leagueapi_id, null_status, space)
    sAg_zero("total_away_over_65", space)
    sAg_total_away_under_65(leagueapi_id, null_status, space)
    sAg_zero("total_away_under_65", space)
    print("", flush=True)

    # ---------------------------------------------------------- Asian Handicap First Half
    sAg_asian_handicap_first_half_home_min_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_175", space)
    sAg_asian_handicap_first_half_away_min_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_175", space)
    sAg_asian_handicap_first_half_home_min_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_15", space)
    sAg_asian_handicap_first_half_away_min_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_15", space)
    sAg_asian_handicap_first_half_home_min_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_125", space)
    sAg_asian_handicap_first_half_away_min_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_125", space)
    sAg_asian_handicap_first_half_home_min_1(leagueapi_id, null_status, space)

    sAg_zero("asian_handicap_first_half_home_min_1", space)
    sAg_asian_handicap_first_half_away_min_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_1", space)
    sAg_asian_handicap_first_half_home_min_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_075", space)
    sAg_asian_handicap_first_half_away_min_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_075", space)
    sAg_asian_handicap_first_half_home_min_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_05", space)
    sAg_asian_handicap_first_half_away_min_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_05", space)
    sAg_asian_handicap_first_half_home_min_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_min_025", space)
    sAg_asian_handicap_first_half_away_min_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_min_025", space)
    sAg_asian_handicap_first_half_home_plus_0(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_0", space)
    sAg_asian_handicap_first_half_away_plus_0(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_0", space)
    sAg_asian_handicap_first_half_home_plus_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_025", space)
    sAg_asian_handicap_first_half_away_plus_025(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_025", space)
    sAg_asian_handicap_first_half_home_plus_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_05", space)
    sAg_asian_handicap_first_half_away_plus_05(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_05", space)
    sAg_asian_handicap_first_half_home_plus_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_075", space)
    sAg_asian_handicap_first_half_away_plus_075(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_075", space)
    sAg_asian_handicap_first_half_home_plus_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_1", space)
    sAg_asian_handicap_first_half_away_plus_1(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_1", space)
    sAg_asian_handicap_first_half_home_plus_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_125", space)
    sAg_asian_handicap_first_half_away_plus_125(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_125", space)
    sAg_asian_handicap_first_half_home_plus_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_15", space)
    sAg_asian_handicap_first_half_away_plus_15(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_15", space)
    sAg_asian_handicap_first_half_home_plus_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_home_plus_175", space)
    sAg_asian_handicap_first_half_away_plus_175(leagueapi_id, null_status, space)
    sAg_zero("asian_handicap_first_half_away_plus_175", space)
    print("", flush=True)

    # ---------------------------------------------------------- Double Chance - First Half
    sAg_double_chance__first_half_home_draw(leagueapi_id, null_status, space)
    sAg_zero("double_chance__first_half_home_draw", space)
    sAg_double_chance__first_half_home_away(leagueapi_id, null_status, space)
    sAg_zero("double_chance__first_half_home_away", space)
    sAg_double_chance__first_half_draw_away(leagueapi_id, null_status, space)
    sAg_zero("double_chance__first_half_draw_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Odd/Even
    sAg_oddeven_odd(leagueapi_id, null_status, space)
    sAg_zero("oddeven_odd", space)
    sAg_oddeven_even(leagueapi_id, null_status, space)
    sAg_zero("oddeven_even", space)
    print("", flush=True)

    # ---------------------------------------------------------- Results/Both Teams Score
    sAg_results_both_teams_score_home_yes(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_home_yes", space)
    sAg_results_both_teams_score_draw_yes(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_draw_yes", space)
    sAg_results_both_teams_score_away_yes(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_away_yes", space)
    sAg_results_both_teams_score_home_no(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_home_no", space)
    sAg_results_both_teams_score_draw_no(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_draw_no", space)
    sAg_results_both_teams_score_away_no(leagueapi_id, null_status, space)
    sAg_zero("results_both_teams_score_away_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Result/Total Goals
    sAg_result_total_goals_home_over_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_home_over_35", space)
    sAg_result_total_goals_draw_over_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_draw_over_35", space)
    sAg_result_total_goals_away_over_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_away_over_35", space)
    sAg_result_total_goals_home_under_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_home_under_35", space)
    sAg_result_total_goals_draw_under_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_draw_under_35", space)
    sAg_result_total_goals_away_under_35(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_away_under_35", space)
    sAg_result_total_goals_home_over_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_home_over_25", space)
    sAg_result_total_goals_draw_over_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_draw_over_25", space)
    sAg_result_total_goals_away_over_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_away_over_25", space)
    sAg_result_total_goals_home_under_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_home_under_25", space)
    sAg_result_total_goals_draw_under_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_draw_under_25", space)
    sAg_result_total_goals_away_under_25(leagueapi_id, null_status, space)
    sAg_zero("result_total_goals_away_under_25", space)
    print("", flush=True)

    # ---------------------------------------------------------- Goals Over/Under - Second Half
    sAg_goals_overunder__second_half_over_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_05", space)
    sAg_goals_overunder__second_half_under_05(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_05", space)
    sAg_goals_overunder__second_half_over_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_075", space)
    sAg_goals_overunder__second_half_under_075(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_075", space)
    sAg_goals_overunder__second_half_over_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_10", space)
    sAg_goals_overunder__second_half_under_10(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_10", space)
    sAg_goals_overunder__second_half_over_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_125", space)
    sAg_goals_overunder__second_half_under_125(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_125", space)
    sAg_goals_overunder__second_half_over_15(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_15", space)
    sAg_goals_overunder__second_half_under_15(leagueapi_id, null_status, space)

    sAg_zero("goals_overunder__second_half_under_15", space)
    sAg_goals_overunder__second_half_over_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_175", space)
    sAg_goals_overunder__second_half_under_175(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_175", space)
    sAg_goals_overunder__second_half_over_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_20", space)
    sAg_goals_overunder__second_half_under_20(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_20", space)
    sAg_goals_overunder__second_half_over_225(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_225", space)
    sAg_goals_overunder__second_half_under_225(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_225", space)
    sAg_goals_overunder__second_half_over_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_25", space)
    sAg_goals_overunder__second_half_under_25(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_25", space)
    sAg_goals_overunder__second_half_over_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_275", space)
    sAg_goals_overunder__second_half_under_275(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_275", space)
    sAg_goals_overunder__second_half_over_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_30", space)
    sAg_goals_overunder__second_half_under_30(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_30", space)
    sAg_goals_overunder__second_half_over_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_325", space)
    sAg_goals_overunder__second_half_under_325(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_325", space)
    sAg_goals_overunder__second_half_over_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_35", space)
    sAg_goals_overunder__second_half_under_35(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_35", space)
    sAg_goals_overunder__second_half_over_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_over_375", space)
    sAg_goals_overunder__second_half_under_375(leagueapi_id, null_status, space)
    sAg_zero("goals_overunder__second_half_under_375", space)
    print("", flush=True)

    # ---------------------------------------------------------- Clean Sheet - Home
    sAg_clean_sheet__home_yes(leagueapi_id, null_status, space)
    sAg_zero("clean_sheet__home_yes", space)
    sAg_clean_sheet__home_no(leagueapi_id, null_status, space)
    sAg_zero("clean_sheet__home_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Clean Sheet - Away
    sAg_clean_sheet__away_yes(leagueapi_id, null_status, space)
    sAg_zero("clean_sheet__away_yes", space)
    sAg_clean_sheet__away_no(leagueapi_id, null_status, space)
    sAg_zero("clean_sheet__away_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Win Both Halves
    sAg_win_both_halves_home(leagueapi_id, null_status, space)
    sAg_zero("win_both_halves_home", space)
    sAg_win_both_halves_away(leagueapi_id, null_status, space)
    sAg_zero("win_both_halves_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Both Teams Score - First Half
    sAg_both_teams_score__first_half_yes(leagueapi_id, null_status, space)
    sAg_zero("both_teams_score__first_half_yes", space)
    sAg_both_teams_score__first_half_no(leagueapi_id, null_status, space)
    sAg_zero("both_teams_score__first_half_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Both Teams To Score - Second Half
    sAg_both_teams_to_score__second_half_yes(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score__second_half_yes", space)
    sAg_both_teams_to_score__second_half_no(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score__second_half_no", space)
    print("", flush=True)

    # ---------------------------------------------------------- Win To Nil
    sAg_win_to_nil_home(leagueapi_id, null_status, space)
    sAg_zero("win_to_nil_home", space)
    sAg_win_to_nil_away(leagueapi_id, null_status, space)
    sAg_zero("win_to_nil_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Exact Goals Number
    sAg_exact_goals_number_0(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_0", space)
    sAg_exact_goals_number_1(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_1", space)
    sAg_exact_goals_number_2(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_2", space)
    sAg_exact_goals_number_3(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_3", space)
    sAg_exact_goals_number_4(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_4", space)
    sAg_exact_goals_number_5(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_5", space)
    sAg_exact_goals_number_6(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_6", space)
    sAg_exact_goals_number_more_7(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number_more_7", space)
    print("", flush=True)

    # ---------------------------------------------------------- To Win Either Half
    sAg_to_win_either_half_home(leagueapi_id, null_status, space)
    sAg_zero("to_win_either_half_home", space)
    sAg_to_win_either_half_away(leagueapi_id, null_status, space)
    sAg_zero("to_win_either_half_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Home Team Exact Goals Number
    sAg_home_team_exact_goals_number_0(leagueapi_id, null_status, space)
    sAg_zero("home_team_exact_goals_number_0", space)
    sAg_home_team_exact_goals_number_1(leagueapi_id, null_status, space)
    sAg_zero("home_team_exact_goals_number_1", space)
    sAg_home_team_exact_goals_number_2(leagueapi_id, null_status, space)
    sAg_zero("home_team_exact_goals_number_2", space)
    sAg_home_team_exact_goals_number_more_3(leagueapi_id, null_status, space)
    sAg_zero("home_team_exact_goals_number_more_3", space)
    print("", flush=True)

    # ---------------------------------------------------------- Away Team Exact Goals Number
    sAg_away_team_exact_goals_number_0(leagueapi_id, null_status, space)
    sAg_zero("away_team_exact_goals_number_0", space)
    sAg_away_team_exact_goals_number_1(leagueapi_id, null_status, space)
    sAg_zero("away_team_exact_goals_number_1", space)
    sAg_away_team_exact_goals_number_2(leagueapi_id, null_status, space)
    sAg_zero("away_team_exact_goals_number_2", space)
    sAg_away_team_exact_goals_number_more_3(leagueapi_id, null_status, space)
    sAg_zero("away_team_exact_goals_number_more_3", space)
    print("", flush=True)

    # ---------------------------------------------------------- Second Half Exact Goals Number
    sAg_second_half_exact_goals_number_0(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_0", space)
    sAg_second_half_exact_goals_number_1(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_1", space)
    sAg_second_half_exact_goals_number_2(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_2", space)
    sAg_second_half_exact_goals_number_3(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_3", space)
    sAg_second_half_exact_goals_number_4(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_4", space)
    sAg_second_half_exact_goals_number_more_5(leagueapi_id, null_status, space)
    sAg_zero("second_half_exact_goals_number_more_5", space)
    print("", flush=True)


    # # ---------------------------------------------------------- Corners Over Under
    # sAg_corners_over_under_over_700(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_700", space)
    # sAg_corners_over_under_under_700(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_700", space)
    # sAg_corners_over_under_over_725(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_725", space)
    # sAg_corners_over_under_under_725(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_725", space)
    # sAg_corners_over_under_over_750(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_750", space)
    # sAg_corners_over_under_under_750(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_750", space)
    # sAg_corners_over_under_over_775(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_775", space)
    # sAg_corners_over_under_under_775(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_775", space)
    # sAg_corners_over_under_over_800(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_800", space)
    # sAg_corners_over_under_under_800(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_800", space)
    # sAg_corners_over_under_over_825(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_825", space)
    # sAg_corners_over_under_under_825(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_825", space)
    # sAg_corners_over_under_over_850(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_850", space)
    # sAg_corners_over_under_under_850(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_850", space)
    # sAg_corners_over_under_over_875(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_875", space)
    # sAg_corners_over_under_under_875(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_875", space)
    # sAg_corners_over_under_over_900(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_900", space)
    # sAg_corners_over_under_under_900(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_900", space)
    # sAg_corners_over_under_over_925(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_925", space)
    # sAg_corners_over_under_under_925(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_925", space)
    # sAg_corners_over_under_over_950(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_950", space)
    # sAg_corners_over_under_under_950(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_950", space)
    # sAg_corners_over_under_over_975(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_975", space)
    # sAg_corners_over_under_under_975(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_975", space)
    # sAg_corners_over_under_over_1000(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1000", space)
    # sAg_corners_over_under_under_1000(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1000", space)
    # sAg_corners_over_under_over_1025(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1025", space)
    # sAg_corners_over_under_under_1025(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1025", space)
    # sAg_corners_over_under_over_1050(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1050", space)
    # sAg_corners_over_under_under_1050(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1050", space)
    # sAg_corners_over_under_over_1075(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1075", space)
    # sAg_corners_over_under_under_1075(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1075", space)
    # sAg_corners_over_under_over_1100(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1100", space)
    # sAg_corners_over_under_under_1100(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1100", space)
    # sAg_corners_over_under_over_1125(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1125", space)
    # sAg_corners_over_under_under_1125(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1125", space)
    # sAg_corners_over_under_over_1150(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1150", space)
    # sAg_corners_over_under_under_1150(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1150", space)
    # sAg_corners_over_under_over_1175(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1175", space)
    # sAg_corners_over_under_under_1175(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1175", space)
    # sAg_corners_over_under_over_1200(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1200", space)
    # sAg_corners_over_under_under_1200(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1200", space)
    # sAg_corners_over_under_over_1225(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1225", space)
    # sAg_corners_over_under_under_1225(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1225", space)
    # sAg_corners_over_under_over_1250(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1250", space)
    # sAg_corners_over_under_under_1250(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1250", space)
    # sAg_corners_over_under_over_1275(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_over_1275", space)
    # sAg_corners_over_under_under_1275(leagueapi_id, null_status, space)
    # sAg_zero("corners_over_under_under_1275", space)
    # print("", flush=True)

    # ---------------------------------------------------------- Exact Goals Number - First Half
    sAg_exact_goals_number__first_half_0(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_0", space)
    sAg_exact_goals_number__first_half_1(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_1", space)
    sAg_exact_goals_number__first_half_2(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_2", space)
    sAg_exact_goals_number__first_half_3(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_3", space)
    sAg_exact_goals_number__first_half_4(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_4", space)
    sAg_exact_goals_number__first_half_more_5(leagueapi_id, null_status, space)
    sAg_zero("exact_goals_number__first_half_more_5", space)
    print("", flush=True)

    # ---------------------------------------------------------- To Score In Both Halves By Teams
    sAg_to_score_in_both_halves_by_teams_home(leagueapi_id, null_status, space)
    sAg_zero("to_score_in_both_halves_by_teams_home", space)
    sAg_to_score_in_both_halves_by_teams_away(leagueapi_id, null_status, space)
    sAg_zero("to_score_in_both_halves_by_teams_away", space)
    print("", flush=True)

    # ---------------------------------------------------------- Total Goals/Both Teams To Score
    sAg_total_goals_both_teams_to_score_over_yes_25(leagueapi_id, null_status, space)
    sAg_zero("total_goals_both_teams_to_score_over_yes_25", space)
    sAg_total_goals_both_teams_to_score_over_no_25(leagueapi_id, null_status, space)
    sAg_zero("total_goals_both_teams_to_score_over_no_25", space)
    sAg_total_goals_both_teams_to_score_under_yes_25(leagueapi_id, null_status, space)
    sAg_zero("total_goals_both_teams_to_score_under_yes_25", space)
    sAg_total_goals_both_teams_to_score_under_no_25(leagueapi_id, null_status, space)
    sAg_zero("total_goals_both_teams_to_score_under_no_25", space)
    print("", flush=True)

    # ---------------------------------------------------------- Halftime Result/Both Teams Score
    sAg_halftime_result_both_teams_score_home_yes(leagueapi_id, null_status, space)

    sAg_zero("halftime_result_both_teams_score_home_yes", space)
    sAg_halftime_result_both_teams_score_draw_yes(leagueapi_id, null_status, space)
    sAg_zero("halftime_result_both_teams_score_draw_yes", space)
    sAg_halftime_result_both_teams_score_away_yes(leagueapi_id, null_status, space)
    sAg_zero("halftime_result_both_teams_score_away_yes", space)
    sAg_halftime_result_both_teams_score_home_no(leagueapi_id, null_status, space)
    sAg_zero("halftime_result_both_teams_score_home_no", space)
    sAg_halftime_result_both_teams_score_draw_no(leagueapi_id, null_status, space)
    sAg_zero("halftime_result_both_teams_score_draw_no", space)
    sAg_halftime_result_both_teams_score_away_no(leagueapi_id, null_status, space)
    sAg_zero("halftime_result_both_teams_score_away_no", space)
    print("", flush=True)

    # # ---------------------------------------------------------- Corners 1x2
    # sAg_corners_winner_home(leagueapi_id, null_status, space)
    # sAg_zero("corners_winner_home", space)
    # sAg_corners_winner_draw(leagueapi_id, null_status, space)
    # sAg_zero("corners_winner_draw", space)
    # sAg_corners_winner_away(leagueapi_id, null_status, space)
    # sAg_zero("corners_winner_away", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap 
    # sAg_corners_asian_handicap_home_min_65(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_65", space)
    # sAg_corners_asian_handicap_away_min_65(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_65", space)
    # sAg_corners_asian_handicap_home_min_6(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_6", space)
    # sAg_corners_asian_handicap_away_min_6(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_6", space)
    # sAg_corners_asian_handicap_home_min_55(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_55", space)
    # sAg_corners_asian_handicap_away_min_55(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_55", space)
    # sAg_corners_asian_handicap_home_min_5(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_5", space)
    # sAg_corners_asian_handicap_away_min_5(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_5", space)
    # sAg_corners_asian_handicap_home_min_45(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_45", space)
    # sAg_corners_asian_handicap_away_min_45(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_45", space)
    # sAg_corners_asian_handicap_home_min_4(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_4", space)
    # sAg_corners_asian_handicap_away_min_4(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_4", space)
    # sAg_corners_asian_handicap_home_min_35(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_35", space)
    # sAg_corners_asian_handicap_away_min_35(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_35", space)
    # sAg_corners_asian_handicap_home_min_3(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_3", space)
    # sAg_corners_asian_handicap_away_min_3(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_3", space)
    # sAg_corners_asian_handicap_home_min_25(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_25", space)
    # sAg_corners_asian_handicap_away_min_25(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_25", space)
    # sAg_corners_asian_handicap_home_min_2(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_2", space)
    # sAg_corners_asian_handicap_away_min_2(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_2", space)
    # sAg_corners_asian_handicap_home_min_15(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_15", space)
    # sAg_corners_asian_handicap_away_min_15(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_15", space)
    # sAg_corners_asian_handicap_home_min_1(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_1", space)
    # sAg_corners_asian_handicap_home_min_05(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_min_05", space)
    # sAg_corners_asian_handicap_away_min_1(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_1", space)
    # sAg_corners_asian_handicap_away_min_05(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_min_05", space)
    # sAg_corners_asian_handicap_home_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_0", space)
    # sAg_corners_asian_handicap_away_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_0", space)
    # sAg_corners_asian_handicap_home_plus_05(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_05", space)
    # sAg_corners_asian_handicap_away_plus_05(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_05", space)
    # sAg_corners_asian_handicap_home_plus_1(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_1", space)
    # sAg_corners_asian_handicap_away_plus_1(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_1", space)
    # sAg_corners_asian_handicap_home_plus_15(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_15", space)
    # sAg_corners_asian_handicap_away_plus_15(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_15", space)
    # sAg_corners_asian_handicap_home_plus_2(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_2", space)
    # sAg_corners_asian_handicap_away_plus_2(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_2", space)
    # sAg_corners_asian_handicap_home_plus_25(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_25", space)
    # sAg_corners_asian_handicap_away_plus_25(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_25", space)
    # sAg_corners_asian_handicap_home_plus_3(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_3", space)
    # sAg_corners_asian_handicap_away_plus_3(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_3", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap
    # sAg_corners_asian_handicap_home_plus_35(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_35", space)
    # sAg_corners_asian_handicap_away_plus_35(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_35", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap 
    # sAg_corners_asian_handicap_home_plus_4(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_4", space)
    # sAg_corners_asian_handicap_away_plus_4(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_4", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap
    # sAg_corners_asian_handicap_home_plus_45(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_45", space)
    # sAg_corners_asian_handicap_away_plus_45(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_45", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap 
    # sAg_corners_asian_handicap_home_plus_5(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_5", space)
    # sAg_corners_asian_handicap_away_plus_5(leagueapi_id, null_status, space)

    # sAg_zero("corners_asian_handicap_away_plus_5", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap
    # sAg_corners_asian_handicap_home_plus_55(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_55", space)
    # sAg_corners_asian_handicap_away_plus_55(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_55", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap 
    # sAg_corners_asian_handicap_home_plus_6(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_6", space)
    # sAg_corners_asian_handicap_away_plus_6(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_6", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Corners Asian Handicap
    # sAg_corners_asian_handicap_home_plus_65(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_home_plus_65", space)
    # sAg_corners_asian_handicap_away_plus_65(leagueapi_id, null_status, space)
    # sAg_zero("corners_asian_handicap_away_plus_65", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Home Corners Over/Under
    # sAg_home_corners_overunder_over_25(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_25", space)
    # sAg_home_corners_overunder_under_25(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_25", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Home Corners Over/Under 
    # sAg_home_corners_overunder_over_35(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_35", space)
    # sAg_home_corners_overunder_under_35(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_35", space)
    # sAg_home_corners_overunder_over_45(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_45", space)
    # sAg_home_corners_overunder_under_45(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_45", space)
    # sAg_home_corners_overunder_over_55(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_55", space)
    # sAg_home_corners_overunder_under_55(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_55", space)
    # sAg_home_corners_overunder_over_65(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_65", space)
    # sAg_home_corners_overunder_under_65(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_65", space)
    # sAg_home_corners_overunder_over_75(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_75", space)
    # sAg_home_corners_overunder_under_75(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_75", space)
    # sAg_home_corners_overunder_over_85(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_over_85", space)
    # sAg_home_corners_overunder_under_85(leagueapi_id, null_status, space)
    # sAg_zero("home_corners_overunder_under_85", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Away Corners Over/Under 
    # sAg_away_corners_overunder_over_25(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_25", space)
    # sAg_away_corners_overunder_under_25(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_25", space)
    # sAg_away_corners_overunder_over_35(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_35", space)
    # sAg_away_corners_overunder_under_35(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_35", space)
    # sAg_away_corners_overunder_over_45(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_45", space)
    # sAg_away_corners_overunder_under_45(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_45", space)
    # sAg_away_corners_overunder_over_55(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_55", space)
    # sAg_away_corners_overunder_under_55(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_55", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Away Corners Over/Under
    # sAg_away_corners_overunder_over_65(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_65", space)
    # sAg_away_corners_overunder_under_65(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_65", space)
    # sAg_away_corners_overunder_over_75(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_75", space)
    # sAg_away_corners_overunder_under_75(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_75", space)
    # sAg_away_corners_overunder_over_85(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_over_85", space)
    # sAg_away_corners_overunder_under_85(leagueapi_id, null_status, space)
    # sAg_zero("away_corners_overunder_under_85", space)
    # print("", flush=True)

    # ---------------------------------------------------------- Both Teams to Score 1st Half - 2nd Half
    sAg_both_teams_to_score_1st_half__2nd_half_yes_yes(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score_1st_half__2nd_half_yes_yes", space)
    sAg_both_teams_to_score_1st_half__2nd_half_yes_no(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score_1st_half__2nd_half_yes_no", space)
    sAg_both_teams_to_score_1st_half__2nd_half_no_yes(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score_1st_half__2nd_half_no_yes", space)
    sAg_both_teams_to_score_1st_half__2nd_half_no_no(leagueapi_id, null_status, space)
    sAg_zero("both_teams_to_score_1st_half__2nd_half_no_no", space)
    print("", flush=True)

    # # ---------------------------------------------------------- 10 Over/Under
    # sAg_10_overunder_over_05(leagueapi_id, null_status, space)
    # sAg_zero("10_overunder_over_05", space)
    # sAg_10_overunder_under_05(leagueapi_id, null_status, space)
    # sAg_zero("10_overunder_under_05", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Last Corner
    # sAg_last_corner_home(leagueapi_id, null_status, space)
    # sAg_zero("last_corner_home", space)
    # sAg_last_corner_away(leagueapi_id, null_status, space)
    # sAg_zero("last_corner_away", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- First Corner
    # sAg_first_corner_home(leagueapi_id, null_status, space)
    # sAg_zero("first_corner_home", space)
    # sAg_first_corner_away(leagueapi_id, null_status, space)
    # sAg_zero("first_corner_away", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Cards European Handicap
    # sAg_cards_european_handicap_home_min_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_home_min_2", space)
    # sAg_cards_european_handicap_draw_min_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_draw_min_2", space)
    # sAg_cards_european_handicap_away_min_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_away_min_2", space)
    # sAg_cards_european_handicap_home_min_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_home_min_1", space)
    # sAg_cards_european_handicap_draw_min_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_draw_min_1", space)
    # sAg_cards_european_handicap_away_min_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_away_min_1", space)
    # sAg_cards_european_handicap_home_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_home_plus_0", space)
    # sAg_cards_european_handicap_draw_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_draw_plus_0", space)
    # sAg_cards_european_handicap_away_plus_0(leagueapi_id, null_status, space)

    # sAg_zero("cards_european_handicap_away_plus_0", space)
    # sAg_cards_european_handicap_home_plus_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_home_plus_1", space)
    # sAg_cards_european_handicap_draw_plus_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_draw_plus_1", space)
    # sAg_cards_european_handicap_away_plus_1(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_away_plus_1", space)
    # sAg_cards_european_handicap_home_plus_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_home_plus_2", space)
    # sAg_cards_european_handicap_draw_plus_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_draw_plus_2", space)
    # sAg_cards_european_handicap_away_plus_2(leagueapi_id, null_status, space)
    # sAg_zero("cards_european_handicap_away_plus_2", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Cards Over/Under
    # sAg_cards_overunder_over_25(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_25", space)
    # sAg_cards_overunder_under_25(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_25", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Cards Over/Under 
    # sAg_cards_overunder_over_35(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_35", space)
    # sAg_cards_overunder_under_35(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_35", space)
    # sAg_cards_overunder_over_45(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_45", space)
    # sAg_cards_overunder_under_45(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_45", space)
    # sAg_cards_overunder_over_55(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_55", space)
    # sAg_cards_overunder_under_55(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_55", space)
    # sAg_cards_overunder_over_65(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_65", space)
    # sAg_cards_overunder_under_65(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_65", space)
    # sAg_cards_overunder_over_75(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_over_75", space)
    # sAg_cards_overunder_under_75(leagueapi_id, null_status, space)
    # sAg_zero("cards_overunder_under_75", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Cards Asian Handicap
    # sAg_cards_asian_handicap_home_min_05(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_home_min_05", space)
    # sAg_cards_asian_handicap_away_min_05(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_away_min_05", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Cards Asian Handicap 
    # sAg_cards_asian_handicap_home_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_home_plus_0", space)
    # sAg_cards_asian_handicap_away_plus_0(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_away_plus_0", space)
    # sAg_cards_asian_handicap_home_plus_05(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_home_plus_05", space)
    # sAg_cards_asian_handicap_away_plus_05(leagueapi_id, null_status, space)
    # sAg_zero("cards_asian_handicap_away_plus_05", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Home Team Total Cards
    # sAg_home_team_total_cards_over_15(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_over_15", space)
    # sAg_home_team_total_cards_under_15(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_under_15", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Home Team Total Cards 
    # sAg_home_team_total_cards_over_25(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_over_25", space)
    # sAg_home_team_total_cards_under_25(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_under_25", space)
    # sAg_home_team_total_cards_over_35(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_over_35", space)
    # sAg_home_team_total_cards_under_35(leagueapi_id, null_status, space)
    # sAg_zero("home_team_total_cards_under_35", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Away Team Total Cards
    # sAg_away_team_total_cards_over_15(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_over_15", space)
    # sAg_away_team_total_cards_under_15(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_under_15", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- Away Team Total Cards 
    # sAg_away_team_total_cards_over_25(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_over_25", space)
    # sAg_away_team_total_cards_under_25(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_under_25", space)
    # sAg_away_team_total_cards_over_35(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_over_35", space)
    # sAg_away_team_total_cards_under_35(leagueapi_id, null_status, space)
    # sAg_zero("away_team_total_cards_under_35", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- RCARD
    # sAg_rcard_yes(leagueapi_id, null_status, space)
    # sAg_zero("rcard_yes", space)
    # sAg_rcard_no(leagueapi_id, null_status, space)
    # sAg_zero("rcard_no", space)
    # print("", flush=True)

    # ---------------------------------------------------------- Total Goals (3 way)
    sAg_total_goals_under_2(leagueapi_id, null_status, space)
    sAg_zero("total_goals_under_2", space)
    sAg_total_goals_2_or_3(leagueapi_id, null_status, space)
    sAg_zero("total_goals_2_or_3", space)
    sAg_total_goals_over_3(leagueapi_id, null_status, space)
    sAg_zero("total_goals_over_3", space)
    print("", flush=True)

    # # ---------------------------------------------------------- First Goal Method
    # sAg_first_goal_method_shot(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_shot", space)
    # sAg_first_goal_method_header(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_header", space)
    # sAg_first_goal_method_penalty(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_penalty", space)
    # sAg_first_goal_method_freekick(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_freekick", space)
    # sAg_first_goal_method_owngoal(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_owngoal", space)
    # sAg_first_goal_method_draw(leagueapi_id, null_status, space)
    # sAg_zero("first_goal_method_draw", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- To Score A Penalty
    # sAg_to_score_a_penalty_home(leagueapi_id, null_status, space)
    # sAg_zero("to_score_a_penalty_home", space)
    # sAg_to_score_a_penalty_away(leagueapi_id, null_status, space)
    # sAg_zero("to_score_a_penalty_away", space)
    # print("", flush=True)

    # # ---------------------------------------------------------- To Miss A Penalty
    # sAg_to_miss_a_penalty_home(leagueapi_id, null_status, space)
    # sAg_zero("to_miss_a_penalty_home", space)
    # sAg_to_miss_a_penalty_away(leagueapi_id, null_status, space)
    # sAg_zero("to_miss_a_penalty_away", space)
 
    # ---------------------------------------------------------- 


def sAg_detail(leagueapi_id, null_status, space):
    # ----------------------------------------------------------   
    # 1a. Match Update
    # ----------------------------------------------------------   
    space += "__"
    # ---------------------------------------------------------- 
    print(space + "sAg_detail()", flush=True)
    # ----------------------------------------------------------  
    host="localhost"
    user="root" 
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ---------------------------------------------------------- 
    query = " Select "
    query += " detail_stats "  
    query += " from football_leagues " 
    query += " where leagueapi_id = '"+str(leagueapi_id)+"' " 
    # ----------------------------------------------------------   
    # print(space + query)
    # ----------------------------------------------------------   
    mycursor = mydb.cursor()
    mycursor.execute(query)
    result =  mycursor.fetchall()
    # ---------------------------------------------------------- 
    total_rows = len(result)
    # print(space + "Total Row(s) : " + str(total_rows), flush=True) 
    # close the cursor and database connection
    mycursor.close()
    mydb.close()
    # ---------------------------------------------------------- 
    counter = 0
    # ----------------------------------------------------------   
    for x in result:    
        # ------------------------------------------------------
        counter        += 1
        detail_stats   = str(x[0])  
    # ----------------------------------------------------------

def sAg_score_secondtime(leagueapi_id, null_status, space):
    # ----------------------------------------------------------   
    # 1a. Match Update
    # ----------------------------------------------------------   
    space += "__"
    # ---------------------------------------------------------- 
    print(space + "sAg_score_secondtime()", flush=True)
    # ----------------------------------------------------------  
    host="localhost"
    user="root" 
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ---------------------------------------------------------- 
    query = " UPDATE `football_statistics` SET "
    query += " `score_secondtime_home` = `goals_home` - `score_halftime_home`, "  
    query += " `score_secondtime_away` = `goals_away` - `score_halftime_away` " 
    query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
    # ----------------------------------------------------------  
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ---------------------------------------------------------- 
    # ---------------------------------------------------------- 
    # ----------------------------------------------------------  
    host="localhost"
    user="root" 
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ---------------------------------------------------------- 
    query = " UPDATE `football_odds` SET "
    query += " `score_secondtime_home` = `goals_home` - `score_halftime_home`, "  
    query += " `score_secondtime_away` = `goals_away` - `score_halftime_away` " 
    query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
    # ----------------------------------------------------------  
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ---------------------------------------------------------- 
    # ---------------------------------------------------------- 

def sAg_zero(col_name, space):
    # ----------------------------------------------------------   
    space += "__"
    # ---------------------------------------------------------- 
    print(space + "sAg_zero()", flush=True)
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `"+col_name+"` = 0 "
    # ----------------------------------------------------------
    query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
    # ---------------------------------------------------------- 
    query += " and (`"+col_name+"` != 1 or `"+col_name+"` is null)"
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # print(query, flush=True)
    # ----------------------------------------------------------

def sAg_match_winner_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_match_winner_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `match_winner_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `match_winner_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    print(query, flush=True)
    # ----------------------------------------------------------

def sAg_match_winner_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_match_winner_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `match_winner_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `match_winner_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    print(query, flush=True)
    # ----------------------------------------------------------

def sAg_match_winner_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_match_winner_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `match_winner_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `match_winner_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    print(query, flush=True)
    # ----------------------------------------------------------

def sAg_first_half_winner_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_first_half_winner_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `first_half_winner_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `first_half_winner_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_first_half_winner_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_first_half_winner_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `first_half_winner_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `first_half_winner_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` = `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_first_half_winner_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_first_half_winner_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `first_half_winner_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `first_half_winner_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_winner_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_winner_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_winner_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_winner_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_secondtime_home` > `score_secondtime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_winner_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_winner_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_winner_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_winner_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_secondtime_home` = `score_secondtime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_winner_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_winner_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_winner_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_winner_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_secondtime_home` < `score_secondtime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_score_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_score_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_score_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` > 0 "
    query += " and `goals_away` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_score_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_score_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_score_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score_yes` = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_homeaway_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_homeaway_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `homeaway_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `homeaway_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` > `goals_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_homeaway_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_homeaway_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `homeaway_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `homeaway_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` < `goals_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance_home_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance_home_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance_home_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance_home_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` >= `goals_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance_home_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance_home_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance_home_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance_home_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` != `goals_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance_draw_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance_draw_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance_draw_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance_draw_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` <= `goals_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_highest_scoring_half_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_highest_scoring_half_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `highest_scoring_half_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `highest_scoring_half_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = (`score_secondtime_home` + `score_secondtime_away`) " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_highest_scoring_half_first(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_highest_scoring_half_first()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `highest_scoring_half_first` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `highest_scoring_half_first` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) > (`score_secondtime_home` + `score_secondtime_away`) " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_highest_scoring_half_second(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_highest_scoring_half_second()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `highest_scoring_half_second` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `highest_scoring_half_second` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) < (`score_secondtime_home` + `score_secondtime_away`) " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_oddeven_odd(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_oddeven_odd()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `oddeven_odd` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `oddeven_odd` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) % 2 = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_oddeven_even(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_oddeven_even()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `oddeven_even` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `oddeven_even` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) % 2 != 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_6` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_6` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_6` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  6)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_575()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_575()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_55()", flush=True)
    # ---------------------------------------------------------- 
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_525` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_525` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_5` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5)  > `goals_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_5` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------

        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_425(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_425(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------

    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_4` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_4` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  4)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------

    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_3` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_3` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  3)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_275` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_225()", flush=True)
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  2)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.75)  > `goals_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------

        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  1)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------

    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------


def sAg_asian_handicap_home_min_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_min_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_min_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_min_025` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_min_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_min_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_min_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_min_025` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` -  0.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_025` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_025` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_05` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  0.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_1()", flush=True)
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.5)  > `goals_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  1.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:

        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)

    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  2.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()

    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_3` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_3` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  3.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_4` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_4` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_425(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_425(leagueapi_id, null_status, space):

    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  4.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_5` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_5` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_525` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_525` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_575()", flush=True)
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_575()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  5.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_6` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_6` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6)  < `goals_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.25)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.25)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.5)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:

        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.5)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_home_plus_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_home_plus_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_home_plus_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_home_plus_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.75)  > `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_away_plus_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_away_plus_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_away_plus_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_away_plus_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` +  6.75)  < `goals_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.75)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.75)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.5)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.5)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.25)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1.25)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"

    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  1)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  0.75)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  0.75)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_05` is Null "
    # ------------------------------------------------------

    query += " and (`score_halftime_home` -  0.5)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  0.5)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_min_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_min_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_min_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_min_025` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  0.25)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_min_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_min_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_min_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_min_025` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` -  0.25)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_0` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_025` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.25)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_025` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.25)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_05(leagueapi_id, null_status, space):

    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.5)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.5)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.75)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  0.75)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.25)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)

    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.25)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.5)  > `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.5)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_home_plus_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_home_plus_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_home_plus_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_home_plus_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.75)  > `score_halftime_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_asian_handicap_first_half_away_plus_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_asian_handicap_first_half_away_plus_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `asian_handicap_first_half_away_plus_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `asian_handicap_first_half_away_plus_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` +  1.75)  < `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_65` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  6.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_65` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  6.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_6` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  6)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_6` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  6)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_55` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  5.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_55` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  5.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_5` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------

    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_5` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_45` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  4.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_45` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  4.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_4` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  4)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------

    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_4` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  4)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_35` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  3.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_35` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  3.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------

        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_3` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  3)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_3` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  3)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_25` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  2.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_25` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  2.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"

    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  2)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  2)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  1.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_15` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  1.5)  < `corner_kicks_away` "

    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  1)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  0.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  1)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` -  0.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  0)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  0)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  0.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_05()", flush=True)
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  0.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  1)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  1)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------

        query += " and `corners_asian_handicap_home_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  1.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_15` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  1.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  2)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  2)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_25` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_25` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  2.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_25` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  2.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_3` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  3)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_3` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  3)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_35(leagueapi_id, null_status, space):

    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_35` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  3.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_35` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  3.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_4` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  4)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "

        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_4` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  4)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_45` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  4.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_45` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  4.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_5` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "

    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_5` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_55` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  5.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_55` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  5.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_6` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  6)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------

    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_6` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  6)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_home_plus_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_home_plus_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_home_plus_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_home_plus_65` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  6.5)  > `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_asian_handicap_away_plus_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_asian_handicap_away_plus_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_asian_handicap_away_plus_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_asian_handicap_away_plus_65` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` +  6.5)  < `corner_kicks_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_home_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_home_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_home_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_home_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  2)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_draw_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_draw_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_draw_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_draw_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` - Draw 2)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_away_min_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_away_min_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_away_min_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_away_min_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  2)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_home_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_home_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_home_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_home_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  1)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_draw_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_draw_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_draw_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_draw_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` - Draw 1)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_away_min_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_away_min_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_away_min_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_away_min_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  1)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_home_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_home_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_home_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_home_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_draw_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_draw_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------

    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_draw_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_draw_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` + Draw 0)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_away_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_away_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_away_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_away_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_home_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_home_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_home_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_home_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  1)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_draw_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_draw_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_draw_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------

        query += " and `cards_european_handicap_draw_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` + Draw 1)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_away_plus_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_away_plus_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_away_plus_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_away_plus_1` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  1)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_home_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_home_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_home_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_home_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  2)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_draw_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_draw_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_draw_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_draw_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` + Draw 2)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_european_handicap_away_plus_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_european_handicap_away_plus_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    query += " `cards_european_handicap_away_plus_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_european_handicap_away_plus_2` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  2)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_home_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_home_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_home_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_home_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  0.5)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_away_min_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_away_min_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_away_min_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_away_min_05` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` -  0.5)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_home_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_home_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_home_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_home_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_away_plus_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_away_plus_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_away_plus_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_away_plus_0` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_home_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_home_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_home_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_home_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0.5)  > `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_asian_handicap_away_plus_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_asian_handicap_away_plus_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_asian_handicap_away_plus_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_asian_handicap_away_plus_05` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home` +  0.5)  < `yellow_cards_away` + `red_cards_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_05` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_075` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_10` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_10` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------

        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_125` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_15` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  1.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"

    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_175` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  1.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_20` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_20` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()

    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_225` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_275` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_30` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_30` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------

    query += " `goals_overunder_over_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_325` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_375()", flush=True)

    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_375` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_40(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_40()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_40` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_40` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  4.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_40(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_40()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_40` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_40` is Null "
    # ------------------------------------------------------

    query += " and (`goals_home` + `goals_away`)  <  4.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_425(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  4.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_425(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_425()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_425` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_425` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  4.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_45` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  4.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_475(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_475()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_475` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_475` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  4.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_50(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_50()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_50` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_50` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  5.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_50(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_50()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()

    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_50` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_50` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  5.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_525` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  5.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_525(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_525()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_525` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_525` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  5.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------

    # ----------------------------------------------------------

def sAg_goals_overunder_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_55` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_575()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  5.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_575(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_575()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_575` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_575` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  5.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_60(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_60()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_60` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_60` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  6.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_60(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_60()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_60` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_60` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  6.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  6.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_625(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_625()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_625` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_625` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  6.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_65` = 1 "
    # ----------------------------------------------------------

    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_65` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  6.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_675(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_675()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_675` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_675` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  6.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_70(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_70()", flush=True)
    # ----------------------------------------------------------
    space += "__"

    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_70` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_70` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  7.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_70(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_70()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_70` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_70` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  7.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_75` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_75` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  7.5 "
    # ----------------------------------------------------------

    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_85` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_85` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_over_95(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_over_95()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_over_95` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_over_95` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  >  9.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_under_95(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_under_95()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_under_95` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_under_95` is Null "
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`)  <  9.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------

        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_10` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_10` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()

    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  1.75 "
    # ----------------------------------------------------------

    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  1.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_20` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_20` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_225` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_225` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_275()", flush=True)
    # ----------------------------------------------------------

    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_275` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_275` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_30` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------

        query += " and `goals_overunder_first_half_under_30` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_325` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_325` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------

    query += " `goals_overunder_first_half_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_over_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_over_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_over_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_over_375` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  >  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder_first_half_under_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder_first_half_under_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder_first_half_under_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder_first_half_under_375` is Null "
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`)  <  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_05` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_075` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  0.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_10` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_10(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_10()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_10` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_10` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  1.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"

    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_125` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  1.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_15` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_175` is Null "

    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  1.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_175` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  1.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_20` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_20(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_20()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_20` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_20` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  2.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_225` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_225` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_225` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  2.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------


def sAg_goals_overunder__second_half_over_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_275` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_275` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  2.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_30` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_30(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_30()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_30` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_30` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  3.0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_325` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_325(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_325()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_325` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_325` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  3.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"

    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_over_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_over_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_over_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_over_375` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  >  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_goals_overunder__second_half_under_375(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_goals_overunder__second_half_under_375()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `goals_overunder__second_half_under_375` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `goals_overunder__second_half_under_375` is Null "
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`)  <  3.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_700(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_700()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_700` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_700` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  7 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_700(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_700()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_700` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_700` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  7 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_725(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_725()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_725` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_725` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  7.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_725(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_725()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_725` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_725` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  7.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_750(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_750()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_750` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_750` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_750(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_750()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_750` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_750` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_775(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_775()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_775` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_775` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  7.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_775(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_775()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------

    query += " `corners_over_under_under_775` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_775` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  7.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_800(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_800()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_800` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_800` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  8 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_800(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_800()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_800` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_800` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  8 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_825(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_825()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_825` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_825` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  8.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_825(leagueapi_id, null_status, space):

    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_825()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_825` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_825` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  8.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_850(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_850()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_850` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_850` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_850(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_850()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_850` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_850` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_875(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_875()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_875` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_875` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  8.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_875(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_875()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_875` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_875` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  8.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_900(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_900()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_900` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_900` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  9 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_900(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_900()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_900` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_900` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  9 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_925(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_925()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_925` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_925` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  9.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_925(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_925()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_925` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_925` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  9.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_950(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_950()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_950` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_950` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  9.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_950(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_950()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_950` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_950` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  9.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_975(leagueapi_id, null_status, space):
    # ----------------------------------------------------------

    space += "__"
    print(space + "sAg_corners_over_under_over_975()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_975` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_975` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  9.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_975(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_975()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_975` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_975` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  9.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1000(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1000()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1000` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1000` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  10 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1000(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1000()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1000` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):

        # ------------------------------------------------------
        query += " and `corners_over_under_under_1000` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  10 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1025` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  10.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1025(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1025()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1025` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1025` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  10.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1050(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1050()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1050` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1050` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  10.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1050(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1050()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1050` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1050` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  10.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1075` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  10.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1075(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1075()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1075` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1075` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  10.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1100(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1100()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1100` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1100` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  11 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1100(leagueapi_id, null_status, space):
    # ----------------------------------------------------------

    space += "__"
    print(space + "sAg_corners_over_under_under_1100()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1100` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1100` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  11 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1125` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  11.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1125(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1125()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1125` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1125` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  11.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1150(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1150()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1150` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------

    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1150` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  11.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1150(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1150()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1150` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1150` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  11.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1175` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  11.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1175(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1175()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1175` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1175` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  11.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1200(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1200()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------

    query += " `corners_over_under_over_1200` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1200` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  12 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1200(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1200()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1200` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1200` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  12 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1225` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  12.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1225(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1225()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1225` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1225` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  12.25 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1250(leagueapi_id, null_status, space):

    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1250()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1250` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1250` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  12.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1250(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1250()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1250` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1250` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  12.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_over_1275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_over_1275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_over_1275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_over_1275` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  >  12.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_over_under_under_1275(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_over_under_under_1275()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_over_under_under_1275` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_over_under_under_1275` is Null "
    # ----------------------------------------------------------
    query += " and (`corner_kicks_home` + `corner_kicks_away`)  <  12.75 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "

        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_45` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_45` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_55` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------

    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_55` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_65` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_65` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_over_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_over_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_over_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_over_75` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  >  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()

    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_cards_overunder_under_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_cards_overunder_under_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `cards_overunder_under_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `cards_overunder_under_75` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `yellow_cards_away` + `red_cards_home` + `red_cards_away`)  <  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_25` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_25` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_35` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_35` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_45` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_45` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_55` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_55` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_55` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_65` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_65` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_75` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_75()", flush=True)

    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_75` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_85` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` >  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_85` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` <  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_25` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_25` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_35` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------

    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_35` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_45` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_45` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_55` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_55` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_55` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_65` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_65` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_75()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_75` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_75(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_75()", flush=True)

    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_75` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_75` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  7.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_over_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_over_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_over_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_over_85` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` >  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_corners_overunder_under_85(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_corners_overunder_under_85()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_corners_overunder_under_85` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_corners_overunder_under_85` is Null "
    # ----------------------------------------------------------
    query += " and `corner_kicks_away` <  8.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_10_overunder_over_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_10_overunder_over_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `10_overunder_over_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `10_overunder_over_05` is Null "
    # ----------------------------------------------------------
    query += " and ( `corner_kicks_home` + `corner_kicks_away`) >  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_10_overunder_under_05(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_10_overunder_under_05()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `10_overunder_under_05` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `10_overunder_under_05` is Null "
    # ----------------------------------------------------------
    query += " and ( `corner_kicks_home` + `corner_kicks_away`) <  0.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance__first_half_home_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance__first_half_home_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance__first_half_home_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance__first_half_home_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` >= `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance__first_half_home_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance__first_half_home_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance__first_half_home_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance__first_half_home_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` != `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_double_chance__first_half_draw_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_double_chance__first_half_draw_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `double_chance__first_half_draw_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `double_chance__first_half_draw_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` <= `score_halftime_away` "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_15` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_15` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_25` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_25` is Null "

    # ----------------------------------------------------------
    query += " and `goals_home` <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_35` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_35` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_45` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') " 
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_45` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_55` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_55` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_over_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_over_65` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_home_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_home_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_home_under_65` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_home_under_65` is Null "
    # ----------------------------------------------------------
    query += " and `goals_home` <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_15` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_15` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_25` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_25` is Null "

    # ------------------------------------------------------
    query += " and `goals_away` <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_35` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_35` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_45` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_45(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_45()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_45` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_45` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` <  4.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_55` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_55(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_55()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_55` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_55` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` <  5.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_over_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_over_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_over_65` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_over_65` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` >  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_away_under_65(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_away_under_65()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_away_under_65` = 1 "

    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_away_under_65` is Null "
    # ------------------------------------------------------
    query += " and `goals_away` <  6.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_clean_sheet__home_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_clean_sheet__home_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `clean_sheet__home_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `clean_sheet__home_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_clean_sheet__home_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_clean_sheet__home_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `clean_sheet__home_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `clean_sheet__home_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_clean_sheet__away_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_clean_sheet__away_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `clean_sheet__away_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `clean_sheet__away_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_clean_sheet__away_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_clean_sheet__away_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `clean_sheet__away_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `clean_sheet__away_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_score__first_half_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_score__first_half_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_score__first_half_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_score__first_half_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` > 0 "
    query += " and `score_halftime_away` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_score__first_half_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_score__first_half_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_score__first_half_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_score__first_half_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score__first_half_yes` = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score__second_half_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score__second_half_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score__second_half_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_to_score__second_half_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_secondtime_home` > 0 "
    query += " and `score_secondtime_home` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score__second_half_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score__second_half_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score__second_half_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_to_score__second_half_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_to_score__second_half_yes` = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_win_to_nil_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_win_to_nil_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `win_to_nil_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `win_to_nil_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` > 0 "
    query += " and `goals_away` = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_win_to_nil_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_win_to_nil_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `win_to_nil_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `win_to_nil_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = 0 "
    query += " and `goals_away` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score_1st_half__2nd_half_yes_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score_1st_half__2nd_half_yes_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score_1st_half__2nd_half_yes_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_to_score_1st_half__2nd_half_yes_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score__first_half_yes` = 1 "
    query += " and `both_teams_to_score__second_half_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score_1st_half__2nd_half_yes_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score_1st_half__2nd_half_yes_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score_1st_half__2nd_half_yes_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_to_score_1st_half__2nd_half_yes_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score__first_half_yes` = 1 "
    query += " and `both_teams_to_score__second_half_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score_1st_half__2nd_half_no_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score_1st_half__2nd_half_no_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score_1st_half__2nd_half_no_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `both_teams_to_score_1st_half__2nd_half_no_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score__first_half_no` = 1 "
    query += " and `both_teams_to_score__second_half_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_both_teams_to_score_1st_half__2nd_half_no_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_both_teams_to_score_1st_half__2nd_half_no_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `both_teams_to_score_1st_half__2nd_half_no_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):

        # ------------------------------------------------------
        query += " and `both_teams_to_score_1st_half__2nd_half_no_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score__first_half_no` = 1 "
    query += " and `both_teams_to_score__second_half_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_under_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_under_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_under_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_under_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) < 2 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_2_or_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_2_or_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_2_or_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_2_or_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) >= 2 "
    query += " and (`goals_home` + `goals_away`) <= 3 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_over_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_over_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_over_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_over_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) > 2 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_rcard_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_rcard_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `rcard_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `rcard_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`red_cards_home` + `red_cards_away`) > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_rcard_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_rcard_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `rcard_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `rcard_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`red_cards_home` + `red_cards_away`) = 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_home_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_home_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_home_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_home_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_draw_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_draw_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_draw_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_draw_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_away_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_away_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_away_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_away_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_home_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_home_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_home_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_home_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ---------------------------------------------------------- 
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_draw_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_draw_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_draw_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_draw_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_results_both_teams_score_away_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_results_both_teams_score_away_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `results_both_teams_score_away_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `results_both_teams_score_away_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_home_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_home_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_home_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_home_over_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `goals_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_draw_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_draw_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_draw_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_draw_over_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `goals_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_away_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_away_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_away_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_away_over_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `goals_overunder_over_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_home_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_home_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_home_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_home_under_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `goals_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query) 
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_draw_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_draw_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_draw_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_draw_under_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `goals_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_away_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_away_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_away_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_away_under_35` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `goals_overunder_under_35` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_home_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_home_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_home_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_home_over_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_draw_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_draw_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_draw_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ---------------------------------------------------------- 
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_draw_over_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_away_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_away_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_away_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_away_over_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_home_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_home_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_home_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_home_under_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_home` = 1 "
    query += " and `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_draw_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_draw_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_draw_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_draw_under_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_draw` = 1 "
    query += " and `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_result_total_goals_away_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_result_total_goals_away_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `result_total_goals_away_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "

        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `result_total_goals_away_under_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `match_winner_away` = 1 "
    query += " and `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_win_both_halves_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_win_both_halves_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `win_both_halves_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `win_both_halves_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `second_half_winner_home` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_win_both_halves_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_win_both_halves_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `win_both_halves_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `win_both_halves_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `second_half_winner_away` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_to_win_either_half_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_to_win_either_half_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `to_win_either_half_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `to_win_either_half_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and ( (`first_half_winner_home` = 1  and `second_half_winner_home` = 0) "
    query += " or (`first_half_winner_home` = 0  and `second_half_winner_home` = 1) ) "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_to_win_either_half_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_to_win_either_half_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `to_win_either_half_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `to_win_either_half_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and ( (`first_half_winner_away` = 1  and `second_half_winner_away` = 0) "
    query += " or (`first_half_winner_away` = 0  and `second_half_winner_away` = 1) ) "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_to_score_in_both_halves_by_teams_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_to_score_in_both_halves_by_teams_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `to_score_in_both_halves_by_teams_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `to_score_in_both_halves_by_teams_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_home` > 0 "
    query += " and `score_secondtime_home` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_to_score_in_both_halves_by_teams_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_to_score_in_both_halves_by_teams_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `to_score_in_both_halves_by_teams_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `to_score_in_both_halves_by_teams_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `score_halftime_away` > 0 "
    query += " and `score_secondtime_away` > 0 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_both_teams_to_score_over_yes_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_both_teams_to_score_over_yes_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_both_teams_to_score_over_yes_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_both_teams_to_score_over_yes_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score_yes` = 1 "
    query += " and `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_both_teams_to_score_over_no_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_both_teams_to_score_over_no_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_both_teams_to_score_over_no_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_both_teams_to_score_over_no_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score_no` = 1 "
    query += " and `goals_overunder_over_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_both_teams_to_score_under_yes_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_both_teams_to_score_under_yes_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_both_teams_to_score_under_yes_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `total_goals_both_teams_to_score_under_yes_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score_yes` = 1 "
    query += " and `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_total_goals_both_teams_to_score_under_no_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_total_goals_both_teams_to_score_under_no_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `total_goals_both_teams_to_score_under_no_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------ 
        query += " and `total_goals_both_teams_to_score_under_no_25` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `both_teams_score_no` = 1 "
    query += " and `goals_overunder_under_25` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_home_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_home_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_home_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `halftime_result_both_teams_score_home_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_draw_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_draw_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_draw_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `halftime_result_both_teams_score_draw_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_draw` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_away_yes(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_away_yes()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_away_yes` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `halftime_result_both_teams_score_away_yes` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `both_teams_score_yes` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_home_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_home_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_home_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------ 
        query += " and `halftime_result_both_teams_score_home_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_draw_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_draw_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_draw_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `halftime_result_both_teams_score_draw_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_draw` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_halftime_result_both_teams_score_away_no(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_halftime_result_both_teams_score_away_no()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `halftime_result_both_teams_score_away_no` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `halftime_result_both_teams_score_away_no` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `both_teams_score_no` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_winner_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_winner_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_winner_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_winner_home` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` > `corner_kicks_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_winner_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_winner_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_winner_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_winner_draw` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` = `corner_kicks_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_corners_winner_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_corners_winner_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `corners_winner_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `corners_winner_away` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `corner_kicks_home` < `corner_kicks_away` " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_0` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_1` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 1 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 2 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 3 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------

    # ----------------------------------------------------------

def sAg_exact_goals_number_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_4` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 4 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_5` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 5 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_6(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_6()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_6` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_6` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) = 6 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number_more_7(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number_more_7()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number_more_7` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number_more_7` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`goals_home` + `goals_away`) >= 7 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ---------------------------------------------------------- 
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_exact_goals_number_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_exact_goals_number_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_exact_goals_number_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_exact_goals_number_0` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_exact_goals_number_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_exact_goals_number_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_exact_goals_number_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_exact_goals_number_1` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = 1 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_exact_goals_number_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_exact_goals_number_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_exact_goals_number_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_exact_goals_number_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` = 2 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_exact_goals_number_more_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_exact_goals_number_more_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_exact_goals_number_more_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_exact_goals_number_more_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_home` >= 3 " 
    # ----------------------------------------------------------
    mycursor.execute(query) 
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_exact_goals_number_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_exact_goals_number_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_exact_goals_number_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_exact_goals_number_0` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_exact_goals_number_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_exact_goals_number_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_exact_goals_number_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_exact_goals_number_1` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` = 1 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_exact_goals_number_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_exact_goals_number_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_exact_goals_number_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_exact_goals_number_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` = 2 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_exact_goals_number_more_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_exact_goals_number_more_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_exact_goals_number_more_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_exact_goals_number_more_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and `goals_away` >= 3 " 
    # ----------------------------------------------------------
    mycursor.execute(query) 
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_0` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_1` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = 1 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = 2 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = 3 " 
    # ---------------------------------------------------------- 
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_4` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) = 4 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_exact_goals_number__first_half_more_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_exact_goals_number__first_half_more_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `exact_goals_number__first_half_more_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `exact_goals_number__first_half_more_5` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_halftime_home` + `score_halftime_away`) >= 5 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_0(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_0()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_0` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_0` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) = 0 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_1(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_1()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_1` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_1` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) = 1 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_2(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_2()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_2` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_2` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) = 2 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_3(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_3()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_3` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_3` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) = 3 " 
    # ---------------------------------------------------------- 
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_4(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_4()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_4` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_4` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) = 4 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_second_half_exact_goals_number_more_5(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_second_half_exact_goals_number_more_5()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `second_half_exact_goals_number_more_5` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `second_half_exact_goals_number_more_5` is Null "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    query += " and (`score_secondtime_home` + `score_secondtime_away`) >= 5 " 
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_over_15` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_under_15` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "

        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_home_team_total_cards_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_home_team_total_cards_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `home_team_total_cards_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `home_team_total_cards_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_home` + `red_cards_home`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_over_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_over_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_over_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_over_15` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  >  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_under_15(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_under_15()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_under_15` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_under_15` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  <  1.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_over_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_over_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_over_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_over_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  >  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_under_25(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_under_25()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_under_25` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "

        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_under_25` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  <  2.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_over_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_over_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_over_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_over_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  >  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_away_team_total_cards_under_35(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_away_team_total_cards_under_35()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `away_team_total_cards_under_35` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `away_team_total_cards_under_35` is Null "
    # ----------------------------------------------------------
    query += " and (`yellow_cards_away` + `red_cards_away`)  <  3.5 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_home_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_home_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_home_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_home_home` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `second_half_winner_home` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_home_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_home_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_home_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_home_draw` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `second_half_winner_draw` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_home_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_home_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_home_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_home_away` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_home` = 1 "
    query += " and `second_half_winner_away` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_draw_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_draw_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_draw_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_draw_home` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_draw` = 1 "
    query += " and `second_half_winner_home` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------

    # ----------------------------------------------------------

def sAg_htft_double_draw_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_draw_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_draw_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_draw_draw` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_draw` = 1 "
    query += " and `second_half_winner_draw` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_draw_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_draw_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_draw_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_draw_away` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_draw` = 1 "
    query += " and `second_half_winner_away` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_away_home(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_away_home()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_away_home` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_away_home` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `second_half_winner_home` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_away_draw(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_away_draw()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_away_draw` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_away_draw` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `second_half_winner_draw` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ---------------------------------------------------------- 
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------

def sAg_htft_double_away_away(leagueapi_id, null_status, space):
    # ----------------------------------------------------------
    space += "__"
    print(space + "sAg_htft_double_away_away()", flush=True)
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    host="localhost"
    user="root"
    database="pr_mmbuzz_2022_06"
    mydb = mysql.connector.connect(host=host,user=user,password="",database=database)
    mycursor = mydb.cursor()
    # ----------------------------------------------------------
    query = " UPDATE `football_statistics` SET  "
    # ----------------------------------------------------------
    space += "__"
    # ----------------------------------------------------------
    query += " `htft_double_away_away` = 1 "
    # ----------------------------------------------------------
    if(leagueapi_id == 0):
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
    else:
        # ------------------------------------------------------
        query += " where fixture_status in ('Match Finished', 'Match Finished Ended') "
        # ------------------------------------------------------
        query += " and leagueapi_id = '"+str(leagueapi_id)+"' "
        # ------------------------------------------------------
    # ----------------------------------------------------------
    if(null_status == 1):
        # ------------------------------------------------------
        query += " and `htft_double_away_away` is Null "
    # ----------------------------------------------------------
    query += " and `first_half_winner_away` = 1 "
    query += " and `second_half_winner_away` = 1 "
    # ----------------------------------------------------------
    mycursor.execute(query)
    mydb.commit()
    # ----------------------------------------------------------
    mycursor.close()
    mydb.close()
    # ----------------------------------------------------------
    # ----------------------------------------------------------





